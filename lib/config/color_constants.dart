import 'package:flutter/material.dart';

/// App 统一颜色常量 — 与 Web 版一致
class AppColors {
  AppColors._();

  // 背景
  static const bg = Color(0xFF1a1d23);
  static const surface = Color(0xFF242830);
  static const surface2 = Color(0xFF2a2e38);
  static const border = Color(0xFF3a3f4a);

  // 文字
  static const text = Color(0xFFd0d4dc);
  static const text2 = Color(0xFF9098a6);

  // 主题色
  static const gold = Color(0xFFc8a04a);
  static const gold2 = Color(0xFFb8943e);
  static const warning = Color(0xFFd4a017);
  static const danger = Color(0xFFe05555);
  static const success = Color(0xFF5a9e5f);
  static const info = Color(0xFF7a8a9a);
}
